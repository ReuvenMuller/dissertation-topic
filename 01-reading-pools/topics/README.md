# Topic paper pools

Each active topic gets a Markdown pool here, named after the topic folder in `03-topics/`.

Copy [templates/topic-paper-pool.md](../../templates/topic-paper-pool.md) when a topic becomes concrete enough to deserve its own reading queue.

Keep exploratory papers in the initial pool or inbox until then.

